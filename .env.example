# AWS Configuration for Bedrock
# IAM role and region are now centralized in ec2_service_role.yml
# You can override them here if needed for specific environments

# Option 1: Use AWS Profile (Local Development)
# AWS_PROFILE will be loaded from ec2_service_role.yml if not set here
# AWS_PROFILE=devops-genai-dna-dev-engineer-role
AWS_DEFAULT_REGION=us-east-1

# Option 2: Use Access Keys (EC2/Docker Deployment)
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here
# AWS_DEFAULT_REGION=us-east-1

# Option 3: Use EC2 IAM Role (Recommended for EC2)
# Just set AWS_DEFAULT_REGION, credentials auto-detected from instance role
# AWS_DEFAULT_REGION=us-east-1

# Flask Secret Key (Change in production!)
SECRET_KEY=your-secret-key-change-this-in-production

# RSS Scheduler Settings (optional)
RSS_SCHEDULE_HOUR=9
RSS_SCHEDULE_MINUTE=0