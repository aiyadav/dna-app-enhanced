# AWS Configuration for Bedrock
# The app auto-detects whether it's running on EC2 or locally
# No manual configuration needed - just set AWS_DEFAULT_REGION

# AWS Region (Required)
AWS_DEFAULT_REGION=us-east-1

# Local Development: Set your AWS profile name
# The app will automatically use this profile when running locally
# On EC2, this is ignored and instance role is used automatically
AWS_PROFILE=devops-genai-dna-dev-engineer-role

# Alternative: Use Access Keys (not recommended for local dev)
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Flask Secret Key
SECRET_KEY=your-secret-key-change-this-in-production

# RSS Scheduler Settings (optional)
RSS_SCHEDULE_HOUR=9
RSS_SCHEDULE_MINUTE=0